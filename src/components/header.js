import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>Store Name</h1>
            <input type="text" placeholder="Search..." />
        </header>
    );
};

export default Header;